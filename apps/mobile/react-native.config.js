const path = require('path');

module.exports = {
  project: {
    android: {
      sourceDir: './android',
    },
  },
  reactNativePath: path.resolve(__dirname, '../../node_modules/react-native'),
};
